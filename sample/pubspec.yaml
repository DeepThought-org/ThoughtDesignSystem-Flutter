name: sample
description: Sample project for Thought design system
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  thought_design_system:
    path: ../thought_design_system

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
